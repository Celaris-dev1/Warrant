[project]
name = "warrant-sdk"
version = "0.1.0"
description = "Python SDK for the Warrant capability broker and PEP"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
